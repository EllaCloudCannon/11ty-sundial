<section class="paragraph">
  <div class="container">
    <div class="row">
      <div class="col" style="width: 100%">
        <div class="paragraph-item-content">
          <h3><span>{{ title }}</span> {{ title_suffix }}</h3>
          <p>{{description}}</p>
          <div class="Learn-more">
            <a href="{{button.link}}" class="">
              {{button.text}}
              <i class="ph-arrow-right"></i>
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>